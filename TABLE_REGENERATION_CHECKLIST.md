# Table Regeneration Checklist

**Purpose:** Inventory of all contaminated result files that must be deleted and regenerated
under the unified inter-patient protocol. Provides the contamination reason and the specific
script that regenerates each file.

**Key:** Files marked IMPOSSIBLE contain values that violate physics/math and must be
deleted before submission. Files marked STALE contain valid-looking numbers from the wrong
experimental setup and must be replaced.

---

## Part 1: Contaminated CSV Tables

### 1.1 `results/tables/table5_consolidated.csv` — **IMPOSSIBLE, DELETE FIRST**

| Column | Source script | Regime | Metric |
|--------|--------------|--------|--------|
| Clean F1 (0.5644) | `report_results.py:309` | Intra model + intra data | **5-class** macro F1 |
| FGSM-F1 (0.8425) | `evaluate_fgsm.py:101` | Inter model + inter data | 4-class macro F1 |
| PGD-F1 (0.8391) | `evaluate_pgd.py:120` | Inter model + inter data | 4-class macro F1 |

**Why it is impossible:** FGSM-drop = 0.56 − 0.84 = **−0.28** (negative drop = adversarial
attack improves F1, which is mathematically impossible for the same model on the same data).
The column mismatch is caused by 5-class vs 4-class F1, not by the attack.

**Action:** Delete. Regenerated by `generate_results_tables.py` after all Phase 0 code fixes
and Phases 3–5 of `RETRAINING_CHECKLIST.md` are complete.

- [ ] Delete current file
- [ ] Regenerate after RETRAINING_CHECKLIST Phase 7.1

---

### 1.2 `results/tables/final_results.csv` — **STALE**

| Model | Current F1 | Issue |
|-------|-----------|-------|
| HMR-BiLSTM | 0.5644 | Intra-patient model on intra-patient data, 5-class F1 |
| LSTM, BiLSTM | (values reflect 12-epoch baselines, inter-patient data) | Epoch parity mismatch |

**Action:** Regenerated by `generate_results_tables.py` from `results/logs/baseline_results.json`
after Phases 2–3 are complete.

- [ ] Delete current file
- [ ] Regenerate after RETRAINING_CHECKLIST Phases 2.2 and 3.1

---

### 1.3 `results/tables/baseline_full_comparison.csv` — **STALE**

Current HMR-BiLSTM F1 = 0.8825 was produced under a regime inconsistent with the canonical
protocol (baseline epoch count mismatch; class weight source mismatch).

**Action:** Regenerated by `generate_results_tables.py` after Phases 2.1 and 2.2 are complete.

- [ ] Delete current file
- [ ] Regenerate after RETRAINING_CHECKLIST Phases 2.1 and 2.2

---

### 1.4 `results/tables/ablation_table_final.csv` — **STALE**

HMR-BiLSTM F1-Clean = 0.8921. Discrepancy from `baseline_full_comparison.csv` (0.8825)
suggests a different checkpoint or a different normalization was used. Ablation must be re-run
from the same canonical checkpoint after Phase 2.1.

**Action:** Regenerated by `run_ablation.py`.

- [ ] Delete current file
- [ ] Regenerate after RETRAINING_CHECKLIST Phase 7.2

---

### 1.5 `results/tables/ablation_robustness.csv` — **STALE**

Same checkpoint issue as `ablation_table_final.csv`. Adversarial ablation results are
inconsistent with the canonical evaluation.

**Action:** Regenerated by `run_ablation.py`.

- [ ] Delete current file
- [ ] Regenerate after RETRAINING_CHECKLIST Phase 7.2

---

### 1.6 `results/tables/fgsm_results.csv` — **May be valid**

Generated by `evaluate_fgsm.py`, which already uses `INTER_TEST` and 4-class F1. However,
this file must be regenerated after `inter_best_rlstm.pt` is retrained (Phase 2.1) to ensure
it reflects the final canonical checkpoint rather than a mid-development checkpoint.

**Action:** Regenerate by `evaluate_fgsm.py` after Phase 2.1.

- [ ] Regenerate after RETRAINING_CHECKLIST Phase 4.1

---

### 1.7 `results/tables/fgsm_baseline_comparison.csv` — **May be valid**

Same rationale as 1.6. Must be regenerated after Phase 2.1 and 2.2 (baselines re-trained).

- [ ] Regenerate after RETRAINING_CHECKLIST Phase 4.1

---

### 1.8 `results/tables/fgsm_baseline_summary.csv` — **May be valid**

Same rationale as 1.6.

- [ ] Regenerate after RETRAINING_CHECKLIST Phase 4.1

---

### 1.9 `results/tables/fgsm_comparison_table.csv` — **Stale**

- [ ] Regenerate after RETRAINING_CHECKLIST Phase 4.1

---

### 1.10 `results/tables/table2_fgsm_robustness.csv` and `.tex` — **Stale**

Downstream of `fgsm_baseline_summary.csv`. Regenerated by `generate_results_tables.py`.

- [ ] Regenerate after RETRAINING_CHECKLIST Phase 7.1

---

### 1.11 `results/tables/table3_calibration.csv` and `.tex` — **Stale**

Must be regenerated after `evaluate_calibration.py` re-runs with the retrained checkpoint.

- [ ] Regenerate after RETRAINING_CHECKLIST Phase 5.1 then 7.1

---

### 1.12 `results/tables/table4_pgd_robustness.csv` and `.tex` — **Stale**

Regenerated by `evaluate_pgd.py` then `generate_results_tables.py`.

- [ ] Regenerate after RETRAINING_CHECKLIST Phases 4.2 and 7.1

---

### 1.13 `results/tables/calibration_results.csv` — **Stale**

Must be regenerated from the retrained inter-patient checkpoint.

- [ ] Regenerate after RETRAINING_CHECKLIST Phase 5.1

---

### 1.14 `results/tables/pgd_baseline_comparison.csv` — **May be valid**

Same rationale as 1.6: uses correct splits but depends on current checkpoint.

- [ ] Regenerate after RETRAINING_CHECKLIST Phase 4.2

---

## Part 2: Contaminated Figures

### 2.1 `results/figures/confusion_matrix.png` — **STALE**

Generated by `report_results.py` from intra-patient model + intra-patient data.  
**Action:** Regenerate by `report_results.py` after Phase 0 fixes and Phase 3.1.

- [ ] Regenerate after RETRAINING_CHECKLIST Phase 3.1

---

### 2.2 `results/figures/roc_curve.png` — **STALE**

Same source as 2.1.

- [ ] Regenerate after RETRAINING_CHECKLIST Phase 3.1

---

### 2.3 `results/figures/comparison_bars.png` — **STALE / IMPOSSIBLE**

Bar heights include HMR-BiLSTM F1=0.5644 (wrong metric/data) and may be inconsistent with
baseline bars (which come from inter-patient 4-class F1).

- [ ] Regenerate after RETRAINING_CHECKLIST Phase 3.1

---

### 2.4 `results/figures/final_results_table.png` — **STALE / IMPOSSIBLE**

Same source. Contains the contaminated F1 values.

- [ ] Regenerate after RETRAINING_CHECKLIST Phase 3.1

---

### 2.5 `results/figures/gate_trajectories.png` — **STALE**

Shows gate trajectories on intra-patient test samples. Should be regenerated on inter-patient
test samples for a representative figure.

- [ ] Regenerate after RETRAINING_CHECKLIST Phase 3.1

---

### 2.6 `results/figures/trustworthy_ai_summary.png` — **STALE**

Aggregates metrics from multiple contaminated sources. Must be regenerated after all
evaluation phases are complete.

- [ ] Regenerate after RETRAINING_CHECKLIST Phase 7.1

---

### 2.7 `results/figures/fgsm_*.png` (all FGSM figures) — **Stale**

Regenerated by `evaluate_fgsm.py` after Phase 2.1.

- [ ] Regenerate after RETRAINING_CHECKLIST Phase 4.1

---

### 2.8 `results/figures/pgd_*.png` (all PGD figures) — **Stale**

- [ ] Regenerate after RETRAINING_CHECKLIST Phase 4.2

---

### 2.9 `results/figures/ablation_*.png` — **Stale**

- [ ] Regenerate after RETRAINING_CHECKLIST Phase 7.2

---

### 2.10 `results/figures/case_*.png` — **Stale**

Generated by `plot_case_visualization.py` from intra-patient test set. After Phase 0 fix,
will show inter-patient test examples.

- [ ] Regenerate after RETRAINING_CHECKLIST Phase 7.3

---

### 2.11 `results/figures/robustness_noise_all.png` and `robustness_summary.png` — **Stale**

The F1 metric (issue #21) was fixed in the code (phase 0) but the figures must be regenerated.

- [ ] Regenerate after RETRAINING_CHECKLIST Phase 4.5

---

### 2.12 `results/figures/reliability_diagram.png` — **Stale**

Must be regenerated from the retrained checkpoint.

- [ ] Regenerate after RETRAINING_CHECKLIST Phase 5.1

---

## Part 3: Contaminated JSON Outputs

### 3.1 `outputs/v1.0_FINAL/robustness/autoattack_results.json` — **INVALID**

Pre-filtered n=200 subset with `clean_accuracy=1.0`. F1 drops are not comparable to FGSM/PGD.  
**Action:** Delete. Regenerate by `evaluate_autoattack.py` after Phase 0 fix removes pre-filtering.

- [ ] Delete current file
- [ ] Regenerate after RETRAINING_CHECKLIST Phase 4.3

---

### 3.2 `outputs/v1.0_FINAL/robustness/cw_attack_results.json` — **INVALID**

Same pre-filtering issue as 3.1.  
**Action:** Delete. Regenerate by `robustness/cw_attack.py` after Phase 0 fix.

- [ ] Delete current file
- [ ] Regenerate after RETRAINING_CHECKLIST Phase 4.4

---

### 3.3 `results/logs/baseline_results.json` — **STALE**

Contains `"hmr_bilstm"` entry with 5-class F1 = 0.5644 from intra-patient model/data.  
Will be overwritten correctly by `report_results.py` and `run_baselines.py` after Phase 0 fixes.

- [ ] Regenerate after RETRAINING_CHECKLIST Phases 2.2 and 3.1

---

## Part 4: Files That Are Already Clean (No Action Needed)

These files were already corrected in session 2 or use the canonical protocol throughout:

| File | Status |
|------|--------|
| `data/processed/splits/inter_*.npz` | Canonical data (regenerate in Phase 1.1 if raw data changes) |
| `outputs/v1.0_FINAL/explainability/shap_importance_ranking.csv` | Uses INTER_TEST throughout |
| `outputs/v1.0_FINAL/explainability/ig_importance.npy` | Uses INTER_TEST throughout |
| MC Dropout uncertainty outputs | Uses INTER_TEST throughout |
| Deep Ensemble outputs | Uses single checkpoint (issue #17, separate decision needed) |

---

## Summary — Deletion Priority

Files that **must be deleted** before submitting (not just replaced — their current content
is scientifically incorrect and could inadvertently end up in the paper):

1. `results/tables/table5_consolidated.csv` — IMPOSSIBLE values
2. `results/tables/table5_consolidated.tex` — same
3. `outputs/v1.0_FINAL/robustness/autoattack_results.json` — n=200 filtered subset
4. `outputs/v1.0_FINAL/robustness/cw_attack_results.json` — n=200 filtered subset

All other contaminated files can be overwritten in-place as each retraining step completes.

---

*Generated: 2026-06-24 (session 2 — Part 2)*
