torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
imbalanced-learn>=0.11.0

# --- dependencies used throughout the codebase but previously missing ---
pyyaml>=6.0.0          # configs/experiment_config.yaml (loaded in virtually every module)
scipy>=1.10.0          # calibration stats, signal processing
shap>=0.43.0           # explainability/shap_analysis.py (T2)
captum>=0.6.0          # explainability/integrated_gradients.py (T3, IntegratedGradients)
torchattacks>=3.5.0    # robustness/auto_attack.py (AutoAttack wrapper)
pyautoattack>=0.1.0      # evaluate_autoattack.py (parameter-free ensemble attack)
python-docx>=1.1.0     # HMR-BiLSTM_Installation_Guide.docx generation
wfdb>=4.1.0            # validation/download_mitdb.py, preprocess_aami.py (PhysioNet records)
tqdm>=4.65.0           # progress bars used in several evaluation scripts
